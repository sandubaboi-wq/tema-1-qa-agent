# Tema 1 — Agent QA cu Tools + Prompts
# Curs AI Agent Development @ Skillab

# Validare parametri + JSON Schema pentru tools
pydantic>=2.0,<3.0

# Prompt templating (YAML + Jinja2)
pyyaml>=6.0
jinja2>=3.1

# Client OpenAI-compatible (funcționează cu LiteLLM, Anthropic, Ollama via proxy)
openai>=2.0

# Cititul .env pentru cheile API
python-dotenv>=1.0

# Fusuri orare IANA pe Windows (Linux/macOS au baza nativ)
tzdata
